# Core dependencies
fastapi==0.115.0
uvicorn==0.30.6
httpx==0.27.2
python-multipart==0.0.9
pyyaml==6.0.2

# ──────────────────────────────────────────────────────────────────
# FUTURE DEPENDENCIES — uncomment as features are implemented
# ──────────────────────────────────────────────────────────────────

# JWT authentication (for create_token / verify_token in auth.py)
# PyJWT>=2.8.0

# Password hashing (for hash_password / verify_password in auth.py)
# bcrypt>=4.1.0

# Async test framework (for tests/test_auth.py, tests/conftest.py)
# pytest-asyncio>=0.23.0

# PDF generation — Pandoc CLI (not a pip package, install system-wide)
# apt-get install pandoc texlive-xetex

# PDF generation — Python PDF manipulation (for publishers/pdf.py)
# pypdf>=4.0.0

# Markdown conversion (for blog publisher)
# markdown>=3.5.0

# Testing and linting
# pytest>=8.0.0
# pytest-cov>=4.1.0
# ruff>=0.3.0