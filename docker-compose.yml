version: '3.8'

services:
  pressroom:
    image: ghcr.io/zzpanic/pressroom:latest
    ports:
      - "8000:8000"
    env_file:
      - stack.env
    volumes:
      - pressroom-data:/app/data
    restart: unless-stopped

volumes:
  pressroom-data:
    driver: local
